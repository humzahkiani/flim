# Disable all cops by default
AllCops:
  DisabledByDefault: true
  NewCops: disable

# Enable and configure indentation cop
Layout/IndentationWidth:
  Enabled: true
  Width: 4

# Enable the parent namespace to make the indentation cop work
Layout:
  Enabled: true
